你正在分析从叙事文本中提取的主题链（snake）。

**Snake 内容（按时间顺序）：**
{% for node in snake_nodes %}
{{ loop.index }}. [{{ node.label }}]
   {{ node.content }}
{% endfor %}

**任务：**
生成一段简洁的叙事性摘要（2-4句话），要求：
1. 抓住连接这些片段的主要主题
2. 描述进展或发展过程
3. 关注"为什么"而不是仅仅列举事实

**输出格式：**
只提供摘要文本，不要额外的格式。
